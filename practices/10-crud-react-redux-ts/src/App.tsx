import { useState } from "react";
import "./App.css";

function App() {
	const [count, setCount] = useState(0);

	return (
		<h1>Proyecto con Redux + Rome linter + Tremor components + Typescript </h1>
	);
}

export default App;
